<h1>New Photo</h1>

<%= form_for [@mutt, @photo] do |f| %>

    <%= f.label :image %>
    <%= f.file_field :image %>

    Set as profile image <%= f.check_box :profile, {}, true, false %>

  <%= f.submit 'Upload' %>
<% end %>